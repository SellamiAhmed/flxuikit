# Only needed if this docs package lives inside a monorepo alongside your
# component library, e.g.:
#
#   packages/
#     uikit/            <- your component library
#     documentation/     <- this package
#
# If this is a standalone repo just for docs, delete this file.
packages:
  - "packages/*"
